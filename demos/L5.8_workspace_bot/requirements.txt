# Бот-воркспейс (Модуль 5). Минимум зависимостей — чтобы был виден сам харнесс.
openai>=1.40        # клиент к OpenAI-совместимому LLM-роутеру курса
httpx>=0.27         # long polling к Telegram/Max Bot API
pandas>=2.0         # анализ данных внутри инструмента python
matplotlib>=3.7     # графики (агент сохраняет PNG, бот шлёт картинкой)
psycopg2-binary>=2.9  # опционально: инструмент sql к учебному PostgreSQL
openpyxl>=3.1       # опционально: агент может собрать отчёт в .xlsx (кейс 5.4)
